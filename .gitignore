dist/
node_modules/
.astro/

# Local secrets — never commit.
# .dev.vars feeds `wrangler dev`; .env feeds the Supabase CLI and any
# shell tooling. Both hold live keys; only the *.example templates are tracked.
.dev.vars
.env
.env.*
!.env.example

# Wrangler cache
.wrangler/

# OS
.DS_Store

# npm (not used — project uses Bun)
package-lock.json
