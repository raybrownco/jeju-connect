dist/
node_modules/
.astro/

# Local secrets — never commit.
# .env holds live keys for both `wrangler dev` and the Supabase CLI.
# Only .env.example is tracked. .dev.vars stays ignored in case an older
# checkout still has one lying around.
.dev.vars
.env
.env.*
!.env.example

# Wrangler cache
.wrangler/

# OS
.DS_Store

# npm (not used — project uses Bun)
package-lock.json
